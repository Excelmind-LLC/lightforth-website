import { DispatchType, Steps } from "../../types";
import { Card } from "../ui";

export const QuestionTwo = ({
  updateQuestion,
}: {
  updateQuestion: DispatchType<Steps>;
}) => {
  return <Card>Question Two</Card>;
};
